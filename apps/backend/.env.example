# Server Configuration
SERVER_PORT=8080
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=home_garden
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRE_HOUR=24

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081

# AWS/S3 Configuration (Optional - for image upload)
# Leave empty to disable image upload functionality
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=
CLOUDFRONT_URL=
# S3_ENDPOINT is for LocalStack or other S3-compatible services (optional)
S3_ENDPOINT=
