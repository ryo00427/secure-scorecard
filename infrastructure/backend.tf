# =============================================================================
# Terraform Backend Configuration
# =============================================================================
#
# デフォルト: ローカルステート（terraform.tfstate）
#
# チーム開発する場合は、以下のいずれかを使用:
# 1. Terraform Cloud（無料、推奨）- https://app.terraform.io
# 2. S3バックエンド（下記参照）
#
# =============================================================================

# -----------------------------------------------------------------------------
# オプション: S3バックエンド（チーム開発用）
# -----------------------------------------------------------------------------
# 使用する場合:
# 1. 先にS3バケットとDynamoDBテーブルをAWSコンソールで作成
# 2. 下記のコメントを外す
# 3. terraform init -migrate-state を実行
#
# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "home-garden/terraform.tfstate"
#     region         = "ap-northeast-1"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }
